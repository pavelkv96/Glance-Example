<resources>
    <!-- Base application theme. -->
    <style name="Theme.GlanceExample" parent="android:Theme.Material">
        <!-- Primary brand color. -->
        <item name="android:colorPrimary">@color/purple_200</item>
        <item name="android:colorPrimaryDark">@color/purple_700</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?android:attr/colorPrimaryDark</item>
        <!-- Customize your theme here. -->
    </style>
</resources>